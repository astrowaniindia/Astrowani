# Backend base URL the admin dashboard talks to.
# For local dev with the backend on the same machine, localhost is fine.
# To test against a backend on the LAN, set the LAN IP (same convention as the apps' SOCKET_URL).
VITE_API_URL=http://localhost:4500
