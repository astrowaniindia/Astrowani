<resources>
    <!-- Matches ic_launcher.png's own background so the Android 12+ splash icon
         has no visible white halo around it. Also used as the adaptive icon's
         background layer (mipmap-anydpi-v26/ic_launcher.xml) for the same reason. -->
    <color name="splash_background">#090018</color>
    <color name="ic_launcher_background">#090018</color>
</resources>
