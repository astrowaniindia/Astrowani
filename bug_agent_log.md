# Bug-scan agent — running log

State tracking for the `/bug-scan` autonomous agent (`.claude/skills/bug-scan/SKILL.md`).
Each row is one investigated or explicitly-skipped issue. Updated by the agent every run —
don't hand-edit unless correcting a stale status.

| Date | Crash signature | Source | Status | Notes |
|---|---|---|---|---|
| 2026-08-05 | `TypeError: Cannot read property 'map' of undefined` in `formatAuspiciousTime` (Sentry `REACT-NATIVE-1`) — `PanchangScreen.js` crashes because backend's `/api/free-services/panchang` never returns `auspicious_period`/`inauspicious_period` | customer | fixed-pending-review | PR #1: https://github.com/astrowaniindia/Astrowani/pull/1 — frontend defaults both fields to `[]` before `.map()`. Backend still doesn't populate real auspicious/inauspicious period data (upstream Jyotisham field names unverified, that host not reachable from this environment) — flagged in PR for human follow-up. New CI workflow (added on `main` after this PR was branched, picked up via merge) is red on both apps' `npm run lint` — pre-existing repo-wide lint debt (94 errors across dozens of untouched files), not caused by this PR; explained in a PR comment, not fixed here (out of scope). **DUPLICATE**: a separate bug-scan run (recurring 8-hourly cron) opened PR #2 (`fix/panchang-screen-crash`) for the same Sentry issue ~40 min later, because this row hadn't reached `main` yet (only existed on this PR's branch) — the log-based dedup only checks `main`, not open PRs/branches. PR #2's fix is more thorough (also guards `formatPanchangData`'s unguarded array indexing). Cross-linked both PRs; left both open for a human to pick one. **Process gap worth fixing**: dedup should also check open PR titles/branches for the same crash signature, not just `main`'s log. **Update (2026-08-10 run)**: confirmed via the GitHub API that PR #1 was in fact merged 2026-08-05T17:45:30Z (`merged: true`) — this row's status should read `fix-merged`, not `fixed-pending-review`. Not changing it directly in this row because two separate open docs PRs (#3 `docs/bug-scan-log-panchang-resolution` and #5 `docs/bug-scan-log-2026-08-08`) already carry that exact status update plus other pending row edits (REACT-NATIVE-2, vendor FCM issue) — editing it a third time here would just create a third conflicting docs PR. Flagging for a human: merge #3 or #5 (they appear to overlap/duplicate each other, same as the code-PR duplicate above) so the log stops drifting from reality; whichever lands should pick up this row's correction too. |
| 2026-08-10 | `NullPointerException: Attempt to invoke interface method 'void com.facebook.react.uimanager.ReactShadowNode.addChildAt(...)' on a null object reference` (Sentry `react-native` project, issue `7662521488`, no short-id surfaced in the API response) — native Android RN shadow-tree crash while committing a UI update | customer | skipped | https://astrowani.sentry.io/issues/7662521488/ — single occurrence (1 event, 1 user, 2026-08-08T23:56:11Z, none since). Full stack trace is 100% native/framework frames (`Thread`→`MessageQueueThreadImpl`→...→RN `UIImplementation.setChildren`/`ReactShadowNode.addChildAt`) with **zero in-app JS frames**, so there's no application code line to point a fix at. Event is tagged `environment: development` and `isSideLoaded: true`, i.e. this came from a sideloaded dev/debug build, not a confirmed production release install — lowers confidence this represents what real users hit. Breadcrumbs immediately before the crash show a `VirtualizedList: ... slow to update` console warning during a burst of `/api/wallet` polling and two overlapping socket.io polling-transport connections, consistent with the well-documented upstream React Native old-architecture bridge race (a shadow node get committed against after the corresponding view was already torn down under UI-thread contention) — no known clean application-level workaround, and speculatively touching list-rendering code across the app on a single unreproduced event risked a worse regression than the bug itself. Recommend: only act if this recurs (especially on a non-`development`-tagged/production event), and separately consider uploading proper Android source maps for this Sentry project so a future in-app-adjacent crash here is actually symbolicated instead of guessed at. |
