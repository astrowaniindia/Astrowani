# Bug-scan agent — running log

State tracking for the `/bug-scan` autonomous agent (`.claude/skills/bug-scan/SKILL.md`).
Each row is one investigated or explicitly-skipped issue. Updated by the agent every run —
don't hand-edit unless correcting a stale status.

| Date | Crash signature | Source | Status | Notes |
|---|---|---|---|---|
| 2026-08-05 | `TypeError: Cannot read property 'map' of undefined` in `formatAuspiciousTime` (Sentry `REACT-NATIVE-1`) — `PanchangScreen.js` crashes because backend's `/api/free-services/panchang` never returns `auspicious_period`/`inauspicious_period` | customer | fixed-pending-review | PR #1: https://github.com/astrowaniindia/Astrowani/pull/1 — frontend defaults both fields to `[]` before `.map()`. Backend still doesn't populate real auspicious/inauspicious period data (upstream Jyotisham field names unverified, that host not reachable from this environment) — flagged in PR for human follow-up. New CI workflow (added on `main` after this PR was branched, picked up via merge) is red on both apps' `npm run lint` — pre-existing repo-wide lint debt (94 errors across dozens of untouched files), not caused by this PR; explained in a PR comment, not fixed here (out of scope). **DUPLICATE**: a separate bug-scan run (recurring 8-hourly cron) opened PR #2 (`fix/panchang-screen-crash`) for the same Sentry issue ~40 min later, because this row hadn't reached `main` yet (only existed on this PR's branch) — the log-based dedup only checks `main`, not open PRs/branches. PR #2's fix is more thorough (also guards `formatPanchangData`'s unguarded array indexing). Cross-linked both PRs; left both open for a human to pick one. **Process gap worth fixing**: dedup should also check open PR titles/branches for the same crash signature, not just `main`'s log. |
| 2026-08-15 | `ReferenceError: Property 'selectedLanguage' doesn't exist` (Sentry `ASTROWANI-VENDOR-3`, https://astrowani.sentry.io/issues/7671855314/) + `TypeError: Cannot read property 'language' of undefined` (Sentry `ASTROWANI-VENDOR-4`, https://astrowani.sentry.io/issues/7671856331/) — both in `CustomHeader.js` | vendor | skipped | Both events tagged `environment: development`, single device (`sdk_gphone16k_x86_64` emulator), single user, all 3 occurrences within 45s (13:39:01–13:39:46 UTC on 2026-08-14). Root-caused via `git log -S`: commit `696d74d` ("Wire up the vendor app's Hindi/English toggle for real", 2026-08-14 19:11:25 IST = 13:41:25 UTC) is the exact commit that removed `CustomHeader.js`'s local `selectedLanguage` state and replaced it with `React.useContext(LanguageContext)` — landed on `main` ~2 minutes after the last of these 3 crashes. This is the developer's own emulator crashing mid-edit (classic Metro Fast-Refresh artifact: old bytecode referencing a just-deleted `useState`, then briefly reading context before `LanguageProvider` was verified wired in `App.js`), not a production issue — the fix these crashes describe is already on `main`, confirmed by reading current `CustomHeader.js` (line 27: `const {language, changeLanguage, t} = React.useContext(LanguageContext);`, no `selectedLanguage` anywhere in the file or its git history outside this one commit's diff) and `App.js` (`LanguageProvider` wraps `NavigationScreen`). No PR needed. |
| 2026-08-15 | Housekeeping only — no new backend errors (`/api/bug-agent/errors` returned 0 entries), no new customer-app Sentry issues (`REACT-NATIVE-1..5` all already covered by PRs #1/#3/#5/#7/#8/#10, no new events since each PR's last run). Re-verified vendor `ASTROWANI-VENDOR-2` (Fragment restore) is still fixed on `main` (`MainActivity.kt` in both apps already has `super.onCreate(null)`) and `REACT-NATIVE-5`'s `removeClippedSubviews` regression is still present on `main` (`AnimatedAstrologerMarquee.js:167` and `Home.js:1183` both still `true`) — so PR #10 remains valid/non-duplicate and PR #8 remains superseded, exactly as PR #10 already documented. | n/a | investigating | No code changes this run. Still needs a human: (1) resolve the `REACT-NATIVE-1` docs-row pileup across PRs #3/#5/#7/#9 (flagged in 4 consecutive runs now, 08-08/08-10/08-13/08-15 — recommend just merging #9, the newest, and closing #3/#5/#7), (2) close PR #6 (superseded by `main` commit `422131a`) and PR #8 (superseded by open PR #10), (3) review/merge PR #4 (vendor FCM `TOO_MANY_REGISTRATIONS`, still valid, `main` unchanged) and PR #10 (marquee/carousel clipping crash, still valid, `main` unchanged). |
