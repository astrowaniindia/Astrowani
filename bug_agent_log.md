# Bug-scan agent — running log

State tracking for the `/bug-scan` autonomous agent (`.claude/skills/bug-scan/SKILL.md`).
Each row is one investigated or explicitly-skipped issue. Updated by the agent every run —
don't hand-edit unless correcting a stale status.

| Date | Crash signature | Source | Status | Notes |
|---|---|---|---|---|
| 2026-08-05 | `TypeError: Cannot read property 'map' of undefined` in `formatAuspiciousTime` (Sentry `REACT-NATIVE-1`) — `PanchangScreen.js` crashes because backend's `/api/free-services/panchang` never returns `auspicious_period`/`inauspicious_period` | customer | fix-merged | Verified directly against `main` on 2026-08-10: `PanchangScreen.js` now has `Array.isArray()` guards on `auspicious_period`/`inauspicious_period` plus optional-chaining on `nakshatra[0]`/`tithi[0]`/`karana[0]`/`yoga[0]` in `formatPanchangData`. Both PR #1 and PR #2 show `merged:false` via the GitHub API (closed without merge) — the hardening landed on `main` some other way (manual cherry-pick, per PR #5's note). Sentry still lists `REACT-NATIVE-1` as unresolved (last event 2026-08-06, none since) — expected, Sentry doesn't auto-resolve; no action needed. |
| 2026-08-07 | `TypeError: undefined is not a function` in `TabBar` (Sentry `REACT-NATIVE-2`, https://astrowani.sentry.io/issues/7659581920/) — crash tree `MySessionsScreen → MaterialTopTabNavigator/TabView → TabBarTop → TabBar` | customer | investigating | Already logged as `investigating` by the 2026-08-08 run (docs update sitting in still-open PR #5, not yet on `main`). No new occurrences since (last seen 2026-08-07T23:42 UTC, zero events in this run's 24h Sentry window). Not duplicating; carrying the open recommendation forward: symbolicate this project (no source maps uploaded, stack resolves to `index.android.bundle` line 1) before attempting a fix. |
| 2026-08-08 | `NullPointerException: ... ReactShadowNode.addChildAt ... on a null object reference` in `UIImplementation.setChildren` (Sentry `REACT-NATIVE-3`, https://astrowani.sentry.io/issues/7662521488/) | customer | investigating | New this run. Single event, single user, 2026-08-08T23:56 UTC only, none since. All frames are `system-only` (legacy RN bridge internals, e.g. `UIImplementation.java`/`ReactShadowNode`) — confirmed `newArchEnabled=false` in `android/gradle.properties` so the legacy (non-Fabric) shadow-tree path is genuinely live, but there's no in-app frame or repro pattern to point at a specific screen/component, and one occurrence isn't enough to safely characterize the race. Not drafting a speculative fix per SKILL.md guidance — flagging for a human: watch for recurrence, and if it repeats, capture Sentry breadcrumbs (which screen was active) to narrow it down. |
| 2026-08-09 | `RuntimeException: Unable to start activity ... Fragment$InstantiationException: Unable to instantiate fragment com.swmansion.rnscreens.ScreenStackFragment` (Sentry `REACT-NATIVE-4`, https://astrowani.sentry.io/issues/7662591468/), culprit `ScreenFragment.<init>` | customer | fixed-pending-review | New this run. PR: https://github.com/astrowaniindia/Astrowani/pull/6 — well-documented `react-native-screens` Android issue (confirmed `react-native-screens@4.18.0` in use): when the OS kills the app process in the background and later restores the Activity from `savedInstanceState`, it tries to reconstruct the screen-stack's Fragments via reflection, which throws. Standard documented fix (react-native-screens README, react-navigation troubleshooting docs): override `MainActivity.onCreate` to call `super.onCreate(null)` instead of passing through the saved bundle, so Android skips Fragment/View state restoration for this Activity. 2 events, 1 user, 2026-08-09. **Note**: `astrowani_vendors-main`'s `MainActivity.kt` has the identical un-patched pattern and is equally exposed, but there's no crash report against the vendor Sentry project for this signature — left untouched to keep this PR's diff scoped to the actual reported crash; flagged in the PR body as a recommended follow-up for a human to decide whether to pre-emptively patch it too. |
| 2026-08-06 | `Error: [messaging/unknown] ... TOO_MANY_REGISTRATIONS` (Sentry `ASTROWANI-VENDOR-1`, https://astrowani.sentry.io/issues/7655529254/) | vendor | fixed-pending-review | Already covered by open PR #4 (`fix/vendor-fcm-registration-crash`) from the 2026-08-06 run. No new events since (last seen 2026-08-06T03:39 UTC). Not duplicating. |
