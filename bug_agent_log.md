# Bug-scan agent — running log

State tracking for the `/bug-scan` autonomous agent (`.claude/skills/bug-scan/SKILL.md`).
Each row is one investigated or explicitly-skipped issue. Updated by the agent every run —
don't hand-edit unless correcting a stale status.

| Date | Crash signature | Source | Status | Notes |
|---|---|---|---|---|
| 2026-08-05 | `TypeError: Cannot read property 'map' of undefined` in `formatAuspiciousTime` (Sentry `REACT-NATIVE-1`) — `PanchangScreen.js` crashes because backend's `/api/free-services/panchang` never returns `auspicious_period`/`inauspicious_period` | customer | fixed-pending-review | PR #1: https://github.com/astrowaniindia/Astrowani/pull/1 — frontend defaults both fields to `[]` before `.map()`. Backend still doesn't populate real auspicious/inauspicious period data (upstream Jyotisham field names unverified, that host not reachable from this environment) — flagged in PR for human follow-up. New CI workflow (added on `main` after this PR was branched, picked up via merge) is red on both apps' `npm run lint` — pre-existing repo-wide lint debt (94 errors across dozens of untouched files), not caused by this PR; explained in a PR comment, not fixed here (out of scope). **DUPLICATE**: a separate bug-scan run (recurring 8-hourly cron) opened PR #2 (`fix/panchang-screen-crash`) for the same Sentry issue ~40 min later, because this row hadn't reached `main` yet (only existed on this PR's branch) — the log-based dedup only checks `main`, not open PRs/branches. PR #2's fix is more thorough (also guards `formatPanchangData`'s unguarded array indexing). Cross-linked both PRs; left both open for a human to pick one. **Process gap worth fixing**: dedup should also check open PR titles/branches for the same crash signature, not just `main`'s log. NOTE (2026-08-13 run): confirmed via GitHub API PR #1 IS `merged: true` (2026-08-05T17:45:30Z) and `PanchangScreen.js` on `main` already contains PR #2's hardening too — this row's `fixed-pending-review` status is stale and should read `fix-merged`, but is deliberately left untouched here (three open, unmerged docs-only PRs — #3, #5, #7 — already have conflicting pending edits to this exact line; a fifth edit only deepens the pileup). Still needs a human to pick one of #3/#5/#7, merge it, close the rest. |
| 2026-08-13 | `RuntimeException`/`Fragment$InstantiationException` on `ScreenStackFragment` restore after Android kills+restores the process (Sentry `REACT-NATIVE-4` customer, `ASTROWANI-VENDOR-2` vendor) | customer + vendor | fix-merged | Both already fixed directly on `main` in commit `422131a` ("Fix production crash: react-native-screens fragment restoration", 2026-08-13T05:00:27+05:30, both apps' `MainActivity.kt` now call `super.onCreate(null)`) — landed outside the bug-scan PR flow, so neither this log nor open PR #6 knew about it until this run. **PR #6** (`fix/screenstack-fragment-restore-crash`, customer-only, opened 2026-08-10) is now superseded/redundant and will conflict with `main` on the same lines — left a comment on #6 recommending closure, did not close it myself. No new PR opened this run since main already carries the fix for both apps. |
| 2026-08-13 | `TypeError: undefined is not a function` in `TabBar` (Sentry `REACT-NATIVE-2`) | customer | investigating | No new signal this run (still just the 5 events from 2026-08-07 22:19–23:42, nothing since). Unchanged from the 2026-08-08 run's assessment (see PR #5) — not symbolicated, no in-app frame to point to. No action taken. |
| 2026-08-13 | `NullPointerException` in `ReactShadowNode.addChildAt` via `UIImplementation.setChildren` (Sentry `REACT-NATIVE-3`) | customer | skipped | No new signal this run (still the single 2026-08-08 event, `environment: development`/sideloaded). Unchanged from the 2026-08-10 run's assessment (see PR #7). No action taken. |
| 2026-08-13 | `IllegalStateException`/`ClassCastException` in `ReactClippingViewManager.getChildCount` from the Home carousel (Sentry `REACT-NATIVE-5`) | customer | fixed-pending-review | No new occurrences this run (still the single 2026-08-11 event). Already covered by open PR #8 (`removeClippedSubviews={false}`) — confirmed the fix is NOT yet on `main` (`AnimatedAstrologerMarquee.js` still has `removeClippedSubviews={true}`), so PR #8 remains valid/non-redundant. Not duplicated. |
| 2026-08-13 | `TOO_MANY_REGISTRATIONS` FCM error, unhandled rejection from `Registration.js`'s unguarded `getFCMToken` (Sentry `ASTROWANI-VENDOR-1`) | vendor | fixed-pending-review | Occurrence count grew to 7 (was 5), last seen 2026-08-12T11:28 — still recurring since PR #4 is unmerged. Confirmed `main`'s `Registration.js` still has the unguarded local `getFCMToken`/raw `messaging().getToken()` (PR #4's described "before" state), so PR #4 remains valid. Not duplicated. |
