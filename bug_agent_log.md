# Bug-scan agent — running log

State tracking for the `/bug-scan` autonomous agent (`.claude/skills/bug-scan/SKILL.md`).
Each row is one investigated or explicitly-skipped issue. Updated by the agent every run —
don't hand-edit unless correcting a stale status.

| Date | Crash signature | Source | Status | Notes |
|---|---|---|---|---|
| 2026-08-05 | `TypeError: Cannot read property 'map' of undefined` in `formatAuspiciousTime` (Sentry `REACT-NATIVE-1`) — `PanchangScreen.js` crashes because backend's `/api/free-services/panchang` never returns `auspicious_period`/`inauspicious_period` | customer | fixed-pending-review | PR #1: https://github.com/astrowaniindia/Astrowani/pull/1 — frontend defaults both fields to `[]` before `.map()`. Backend still doesn't populate real auspicious/inauspicious period data (upstream Jyotisham field names unverified, that host not reachable from this environment) — flagged in PR for human follow-up. New CI workflow (added on `main` after this PR was branched, picked up via merge) is red on both apps' `npm run lint` — pre-existing repo-wide lint debt (94 errors across dozens of untouched files), not caused by this PR; explained in a PR comment, not fixed here (out of scope). **DUPLICATE**: a separate bug-scan run (recurring 8-hourly cron) opened PR #2 (`fix/panchang-screen-crash`) for the same Sentry issue ~40 min later, because this row hadn't reached `main` yet (only existed on this PR's branch) — the log-based dedup only checks `main`, not open PRs/branches. PR #2's fix is more thorough (also guards `formatPanchangData`'s unguarded array indexing). Cross-linked both PRs; left both open for a human to pick one. **Process gap worth fixing**: dedup should also check open PR titles/branches for the same crash signature, not just `main`'s log. |
| 2026-08-14 | `IllegalStateException`/`ClassCastException: ReactHorizontalScrollView cannot be cast to ReactViewGroup` (Sentry `REACT-NATIVE-5`, `astrowani.sentry.io/issues/7665434814/`) — regressed back onto `main` | customer | fixed-pending-review | This exact crash was already fixed on `main` by commit `1c9863b` (2026-08-11, `removeClippedSubviews={false}` on Home's horizontal lists) — open PR #8 targeting the same line predates that commit and is now redundant/conflicting, see below. Two subsequent commits then **reintroduced** the crash risk: `f01662e` (2026-08-13) set `AnimatedAstrologerMarquee.js`'s `removeClippedSubviews` back to `true` after shrinking its `LOOP_COUNT` buffer, and `b2aff4b` (2026-08-13) did the same for Home.js's top-astrologers carousel after trimming its duplicated array — both under the stated belief that a smaller buffer/array made clipping safe again. That reasoning conflates two unrelated things: the crash is a documented Android RN bug in the clipping/view-recycling reattachment path for a horizontal list nested in a vertical ScrollView and driven by programmatic `scrollToOffset()` auto-advance — it is not caused by item count, so shrinking the buffer doesn't address it. This PR sets both flags back to `false` (with an inline comment on each explaining why, to stop this from flip-flopping a third time) and leaves the buffer-size/perf work from those two commits untouched. PR: (see this run's summary). **Also**: commented on PR #8 recommending it be closed as superseded (base predates the buffer-size commits, would conflict; the fix is now carried by this new PR against current `main` instead). PR #6 (`REACT-NATIVE-4`/`ASTROWANI-VENDOR-2`, `MainActivity.kt`) remains superseded by commit `422131a` per the prior run's note on that PR — unchanged, still needs a human to close it. No new Sentry issues (customer or vendor) or backend errors since the 2026-08-13 run; `REACT-NATIVE-1`'s log-row pileup (PRs #3/#5/#7/#9) also unchanged — still needs a human to pick one and close the rest. |
