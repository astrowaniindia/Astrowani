# Bug-scan agent — running log

State tracking for the `/bug-scan` autonomous agent (`.claude/skills/bug-scan/SKILL.md`).
Each row is one investigated or explicitly-skipped issue. Updated by the agent every run —
don't hand-edit unless correcting a stale status.

| Date | Crash signature | Source | Status | Notes |
|---|---|---|---|---|
| 2026-08-05 | `TypeError: Cannot read property 'map' of undefined` in `formatAuspiciousTime` (Sentry `REACT-NATIVE-1`) — `PanchangScreen.js` crashes because backend's `/api/free-services/panchang` never returns `auspicious_period`/`inauspicious_period` | customer | fix-merged | PR #1 (https://github.com/astrowaniindia/Astrowani/pull/1) merged 2026-08-05. PR #2 (duplicate, https://github.com/astrowaniindia/Astrowani/pull/2) was closed without merging via the GitHub API, but as of this run (2026-08-08) `PanchangScreen.js` on `main` already contains PR #2's more thorough `Array.isArray` guards on `formatAuspiciousTime`/`formatInauspiciousTime` *and* optional-chaining on `formatPanchangData`'s `nakshatra[0]`/`tithi[0]`/`karana[0]`/`yoga[0]` indexing — verified by reading the current file, so the extra hardening landed one way or another (manual cherry-pick outside the PR, most likely) even though PR #2 itself shows unmerged. No further action needed; risk is closed. `REACT-NATIVE-1` still shows a `lastSeen` of 2026-08-06T08:46 (after PR #1's merge) — expected, since RN app code only updates when users get a new build, not on merge. |
| 2026-08-08 | `TypeError: undefined is not a function` inside `TabBar` (Sentry `REACT-NATIVE-2`, https://astrowani.sentry.io/issues/7659581920/) — crash tree is `MySessionsScreen` → `MaterialTopTabNavigator`/`TabView` → `TabBarTop` → `TabBar` (`@react-navigation/material-top-tabs` + `react-native-tab-view` + `react-native-pager-view`) | customer | investigating | 5 events, all within 22:19–23:42 UTC on 2026-08-07, none since (>24h clean as of this run). Single device model in the sampled event (`LAVA LXX505`, Android 14, dist 26, app release `com.astrowanicustomer@24.0+26`) — unclear if all 5 events are the same device/user or several. Read `astrowani_customer-main/src/screens/drawerScreens/MySessionScreen.js` in full: `Tab.Navigator`'s `screenOptions` (incl. `tabBarScrollEnabled: true`) and all 4 `Tab.Screen` options are static values / plain `t(...)` string calls — no custom `tabBarIcon`/`tabBarLabel`/`tabBar` render function in our own code that could throw. The stack trace itself is **not symbolicated** (no source maps configured for this Sentry project — frames are `index.android.bundle` at flat line 1 with huge column offsets, e.g. `callFunctionReturnFlushedQueue`/`__guard`/`receiveTouches`), so I cannot pin the exact throwing line inside the third-party tab-view/pager-view code. Checked installed versions in `package.json` (`@react-navigation/material-top-tabs@^6.6.14`, `react-native-tab-view` (transitive), `react-native-pager-view@^6.3.4`, `react-native-reanimated@3.18.2`, `react-native-gesture-handler@2.24.0`, RN `0.77.2`) — nothing obviously mismatched, but I have no way to run/repro the app in this environment to confirm. **Not confident in a root cause — did not draft a speculative fix.** Two things worth a human's attention: (1) whether to reproduce this on a similar low-end Android 14 device while swiping/tapping the My Sessions tabs, and (2) uploading source maps for the `react-native` Sentry project (`sentry-cli`/`@sentry/react-native` Gradle plugin `uploadSourceMaps`) so future crashes in this project are symbolicated — right now every JS-side crash here resolves to unreadable `index.android.bundle` frames, which will keep blocking root-causing from this environment. |
| 2026-08-08 | `Error: [messaging/unknown] ... TOO_MANY_REGISTRATIONS` in `getToken` (Sentry `ASTROWANI-VENDOR-1`) | vendor | fixed-pending-review | Already covered by open PR #4 (https://github.com/astrowaniindia/Astrowani/pull/4, `fix/vendor-fcm-registration-crash`) from the 2026-08-06 run — not duplicating. PR #4 is still open and unmerged as of this run; no new events since 2026-08-06T03:39, consistent with the crash being a one-time registration-cap burst rather than an ongoing issue. |
