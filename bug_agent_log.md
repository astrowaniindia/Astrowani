# Bug-scan agent — running log

State tracking for the `/bug-scan` autonomous agent (`.claude/skills/bug-scan/SKILL.md`).
Each row is one investigated or explicitly-skipped issue. Updated by the agent every run —
don't hand-edit unless correcting a stale status.

| Date | Crash signature | Source | Status | Notes |
|---|---|---|---|---|
| 2026-08-05 | `TypeError: Cannot read property 'map' of undefined` in `formatAuspiciousTime` (Sentry `REACT-NATIVE-1`, `astrowani.sentry.io/issues/7653834455/`) — `PanchangScreen.js` crashes because backend's `/api/free-services/panchang` never returns `auspicious_period`/`inauspicious_period` | customer | fix-merged | PR #1 merged 2026-08-05: https://github.com/astrowaniindia/Astrowani/pull/1 — frontend defaults both fields to `[]` before `.map()`. Backend still doesn't populate real auspicious/inauspicious period data (upstream Jyotisham field names unverified) — flagged for human follow-up, not done. PR #2 (a same-issue duplicate, see the 2026-08-05 incident note this row used to carry) is closed-not-merged, but its extra `formatPanchangData` array-indexing guards landed on `main` anyway via a manual follow-up commit (`7c07713`), confirmed by the 2026-08-08 run reading the file directly. No new occurrences of this issue since 2026-08-06. **Corrected to `fix-merged` this run** — PRs #3/#5/#7/#9/#11/#12 all separately tried to make this same correction and piled up unmerged; consolidated into this row instead of adding a 7th competing edit. Recommend a human close #3/#5/#7/#9/#11/#12 now that this row reflects reality. |
| 2026-08-11 → 2026-08-16 | `IllegalStateException`/`ClassCastException: ReactHorizontalScrollView cannot be cast to ReactViewGroup` (Sentry `REACT-NATIVE-5`, `astrowani.sentry.io/issues/7665434814/`) in Home's horizontal carousels, nested in a vertical `ScrollView` and auto-advanced via `scrollToOffset()` | customer | fix-merged | Fixed once (`1c9863b`, 2026-08-11), regressed twice on 2026-08-13 (`f01662e`, `b2aff4b` each flipped `removeClippedSubviews` back to `true`, mistakenly believing a smaller buffer/array made clipping safe — it doesn't, this is a clipping-mechanism bug, not an item-count one), then re-fixed and merged via PR #10 (2026-08-16): https://github.com/astrowaniindia/Astrowani/pull/10 — both `AnimatedAstrologerMarquee.js` and `Home.js`'s carousels now carry inline comments explaining why the flag must stay `false`, to stop a third flip-flop. Confirmed on current `main`: both are `removeClippedSubviews={false}`. No new occurrences since the single 2026-08-11 event. PR #8 (the original, now-superseded fix attempt) is still open — recommend a human close it. |
| 2026-08-13 | `RuntimeException`/`Fragment$InstantiationException` on `ScreenStackFragment` restore, at app-launch Activity restoration (Sentry `REACT-NATIVE-4` customer `astrowani.sentry.io/issues/7662591468/`, `ASTROWANI-VENDOR-2` vendor `astrowani.sentry.io/issues/7668770253/`) | both apps | fix-merged | Fixed directly on `main` by commit `422131a` (2026-08-13, outside the bug-scan flow) — both apps' `MainActivity.kt` now override `onCreate` and call `super.onCreate(null)`, skipping native Fragment-tree restoration. Confirmed by reading both files directly this run. PR #6 (an earlier, now-superseded fix attempt for the customer app only) is still open — recommend a human close it. No new occurrences of either issue since 2026-08-14. |
| 2026-08-08 | `TypeError: undefined is not a function` inside `TabBar`, crash tree `MySessionsScreen → MaterialTopTabNavigator/TabView → TabBarTop → TabBar` (Sentry `REACT-NATIVE-2`, `astrowani.sentry.io/issues/7659581920/`) | customer | investigating | 5 events 2026-08-07, none since. `MySessionScreen.js`'s own `Tab.Navigator` config has no custom `tabBar*` render function that could throw, and the Sentry stack isn't symbolicated (no source maps uploaded for this project), so the exact third-party throw site can't be pinned with confidence — no speculative fix drafted. Needs: (a) reproduction attempt on a low-end Android device, (b) source-map upload for the `react-native` Sentry project. Unchanged for four consecutive runs (no new events) — still open, no PR. |
| 2026-08-08 | `NullPointerException` in `ReactShadowNode.addChildAt` via `UIImplementation.setChildren` (Sentry `REACT-NATIVE-3`, `astrowani.sentry.io/issues/7662521488/`) | customer | skipped | Single event, `environment: development`, `isSideLoaded: true` (sideloaded dev build), stack trace is 100% native/framework frames with zero in-app JS frames — nothing in our code to fix. Breadcrumbs point to a known old-architecture RN bridge race under UI-thread contention during heavy wallet-polling + socket activity, which has no clean app-level workaround. No recurrence since. Only worth revisiting if it recurs on a non-development-tagged (real production) event. |
| 2026-08-06 | `Error: [messaging/unknown] ... TOO_MANY_REGISTRATIONS` from unguarded `messaging().getToken()` in vendor `Registration.js` (Sentry `ASTROWANI-VENDOR-1`, `astrowani.sentry.io/issues/7655529254/`) | vendor | investigating | PR #4 (open, unmerged): https://github.com/astrowaniindia/Astrowani/pull/4 — swaps the screen's own unguarded FCM-token fetch for the shared, cached, try/catch-wrapped `getFCMToken` helper in `utils/Firebase.js` that every other call site already uses. Confirmed `main`'s `Registration.js` still has the unguarded version — fix not yet landed. Occurrence count was still climbing as of 2026-08-13 (5→7); no new events since. |
| 2026-08-14 | `ReferenceError: Property 'selectedLanguage' doesn't exist` + `TypeError: Cannot read property 'language' of undefined`, both in vendor `CustomHeader.js` (Sentry `ASTROWANI-VENDOR-3` `astrowani.sentry.io/issues/7671855314/`, `ASTROWANI-VENDOR-4` `astrowani.sentry.io/issues/7671856331/`) | vendor | skipped | All 3 occurrences fall in a 45s window on a single emulator/single user, ~2 minutes before commit `696d74d` landed on `main` (which deletes `CustomHeader.js`'s local `selectedLanguage` state for `useContext(LanguageContext)`) — a Fast-Refresh artifact from the developer's own edit-in-progress, not a real user crash. Confirmed `main` already has the fixed `useContext` version and `selectedLanguage` doesn't appear anywhere in history after that commit. No PR needed, no recurrence. |
| 2026-08-15 | `SMS not delivered: failed` / `SMPP session not connected` (`enablex-sms`, backend error log, one phone number, one event) | backend | skipped | `verifyEnxDelivery` (added by an unrelated commit `31cc15a`, not by this agent) working as designed — it polls EnableX ~20s after an accepted send and logs non-delivery. `SMPP session not connected` is EnableX's own carrier-side link dropping, not app code; no in-repo line to fix. Single event, single number — not enough volume to distinguish a one-off carrier blip from a systemic gateway problem. Revisit only if this recurs at volume across multiple numbers. |
| 2026-08-16 | — no new backend errors or Sentry issues since the 2026-08-15 (run 2) scan (PR #12) — | — | n/a | Backend error log: 1 entry, identical to the SMS-failure event already triaged above (same timestamp/jobId, not new). Sentry customer (`react-native`) and vendor (`astrowani-vendor`) projects: same 5 and 4 `is:unresolved` issues respectively as the prior run, identical counts and `lastSeen` timestamps — no new occurrences on any of them. This run's only work was consolidating the rows above (correcting `REACT-NATIVE-1`/`REACT-NATIVE-5`/`REACT-NATIVE-4`/`ASTROWANI-VENDOR-2` to `fix-merged` and folding six runs' worth of separate skip/investigating notes into single per-issue rows) — see PR opened by this run for the full docs diff. **Housekeeping still needing a human**: close PRs #3, #5, #7, #9, #11, #12 (superseded by this row consolidation), close PR #6 (superseded by commit `422131a`), close PR #8 (superseded by merged PR #10), and review/merge the one remaining still-valid open fix, PR #4 (vendor FCM). |
